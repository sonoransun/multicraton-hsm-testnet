module github.com/craton-co/craton-hsm-go

go 1.21

// Proto-generated gRPC stubs are produced by `go generate ./...` running buf
// against the root repo's craton-hsm-daemon/proto/craton_hsm.proto. Until
// those land in internal/cratonpb, no external modules are required.
